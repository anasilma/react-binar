import React, { Component } from 'react';
import AppContainer, { AppTab } from './navigation/navigation';

export default class App extends Component {
  render() {
      return( <AppContainer /> );
  }
}